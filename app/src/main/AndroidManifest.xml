<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="tech.needsbox.mobile">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
            android:name=".NeedsBoxApplication"
            android:allowBackup="true"
            android:fullBackupContent="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.NeedsBoxMobile"
            android:usesCleartextTraffic="true">
        <activity android:name=".activities.ui.main.MainActivity" />
        <activity
                android:name=".activities.ui.settings.SettingsActivity"
                android:label="@string/title_activity_settings" />

        <meta-data
                android:name="io.sentry.dsn"
                android:value="https://df6d6bfdf2e8436bbf527827c49dc101@o482572.ingest.sentry.io/5636155" />

        <activity
                android:name=".activities.ui.login.LoginActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>